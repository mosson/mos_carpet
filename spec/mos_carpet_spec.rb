require 'spec_helper'

describe MosCarpet do
  it 'has a version number' do
    expect(MosCarpet::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
